import { PhysicsEngine } from '../physics/PhysicsEngine'; import { ChartsManager } from './ChartsManager'; export class UIManager { physMain: PhysicsEngine; physCompare: PhysicsEngine; charts: ChartsManager; elTime = document.getElementById('t-time')!; elRadius = document.getElementById('t-radius')!; elVel = document.getElementById('t-vel')!; elAcc = document.getElementById('t-acc')!; elKin = document.getElementById('t-kin')!; elPot = document.getElementById('t-pot')!; chkHalo = document.getElementById('chk-halo') as HTMLInputElement; chkDisk = document.getElementById('chk-disk') as HTMLInputElement; chkBulge = document.getElementById('chk-bulge') as HTMLInputElement; slSpeed = document.getElementById('sl-speed') as HTMLInputElement; lblSpeed = document.getElementById('lbl-speed')!; btnReset = document.getElementById('btn-reset')!; btnOrbit = document.getElementById('btn-cam-orbit')!; btnSun = document.getElementById('btn-cam-sun')!; btnSplit = document.getElementById('btn-split')!; simSpeed = 5; cameraMode: 'orbit' | 'sun' = 'orbit'; isSplit = false; onResetReq: () => void = () => {}; onSplitToggled: (active: boolean) => void = () => {}; constructor(phys1: PhysicsEngine, phys2: PhysicsEngine) { this.physMain = phys1; this.physCompare = phys2; this.charts = new ChartsManager(); this.chkHalo.addEventListener('change', (e) => { this.physMain.useHalo = (e.target as HTMLInputElement).checked; }); this.chkDisk.addEventListener('change', (e) => { this.physMain.useDisk = (e.target as HTMLInputElement).checked; }); this.chkBulge.addEventListener('change', (e) => { this.physMain.useBulge = (e.target as HTMLInputElement).checked; }); this.slSpeed.addEventListener('input', (e) => { this.simSpeed = parseInt((e.target as HTMLInputElement).value); this.lblSpeed.innerText = this.simSpeed.toString(); }); this.btnReset.addEventListener('click', () => { this.physMain.reset(); this.physCompare.reset(); this.charts.clear(); this.onResetReq(); }); this.btnOrbit.addEventListener('click', () => { this.cameraMode = 'orbit'; this.btnOrbit.classList.add('active'); this.btnSun.classList.remove('active'); }); this.btnSun.addEventListener('click', () => { this.cameraMode = 'sun'; this.btnSun.classList.add('active'); this.btnOrbit.classList.remove('active'); }); this.btnSplit.addEventListener('click', () => { this.isSplit = !this.isSplit; if(this.isSplit) { this.btnSplit.classList.add('active'); document.getElementById('split-divider')?.classList.add('active'); document.getElementById('split-labels')?.classList.remove('hidden'); } else { this.btnSplit.classList.remove('active'); document.getElementById('split-divider')?.classList.remove('active'); document.getElementById('split-labels')?.classList.add('hidden'); } this.onSplitToggled(this.isSplit); }); } updateTelemetry(accMag: number) { let r = Math.sqrt(this.physMain.x**2 + this.physMain.y**2 + this.physMain.z**2); let v = Math.sqrt(this.physMain.vx**2 + this.physMain.vy**2 + this.physMain.vz**2) / this.physMain.KM_S_TO_KPC_GYR; let { Ek, Ep } = this.physMain.getEnergies(); this.elTime.innerHTML = `${this.physMain.t.toFixed(4)} <small>Gyr</small>`; this.elRadius.innerHTML = `${r.toFixed(2)} <small>kpc</small>`; this.elVel.innerHTML = `${v.toFixed(2)} <small>km/s</small>`; this.elAcc.innerHTML = `${accMag.toExponential(2)} <small>kpc/G²</small>`; let sk = Ek.toExponential(2); let sp = Ep.toExponential(2); this.elKin.innerText = sk; this.elPot.innerText = sp; } }